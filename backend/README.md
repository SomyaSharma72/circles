# Neighborly Backend

Express.js REST API server for the Neighborly application.

## Setup & Running

```bash
cd backend
npm install
npm run dev
```

The server will start on `http://localhost:5000`.
