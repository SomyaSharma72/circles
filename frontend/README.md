# Neighborly Frontend

React + Vite + Tailwind CSS frontend for the Neighborly application.

## Getting Started

```bash
npm install
npm run dev
```

The application will be running on `http://localhost:3000`.
